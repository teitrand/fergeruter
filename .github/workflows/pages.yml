name: Publiser til GitHub Pages

# main → https://teitrand.github.io/fergeruter/
# testhost → https://teitrand.github.io/fergeruter/dev/
#
# github-pages-miljøet i dette repoet tillèt berre `main` (ikkje `dev`).
# Køyr derfor berre herifrå main, og hent testhosten frå greina `dev`.
# Fyrste gong: Settings → Pages → Source = GitHub Actions.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main (prod)
        uses: actions/checkout@v4
        with:
          ref: main
          path: prod
      - name: Checkout dev (test)
        uses: actions/checkout@v4
        with:
          ref: dev
          path: staging
        continue-on-error: true
      - name: Assemble site
        run: |
          mkdir -p publish
          rsync -a --exclude '.git' --exclude '.github' --exclude 'dev' prod/ publish/
          if [ -d staging/.git ] || [ -f staging/index.html ]; then
            mkdir -p publish/dev
            rsync -a --exclude '.git' --exclude '.github' --exclude 'dev' staging/ publish/dev/
          fi
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: publish
      - name: Deploy
        id: deploy
        uses: actions/deploy-pages@v4
